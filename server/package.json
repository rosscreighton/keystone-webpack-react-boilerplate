{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:dev": "yarn run build:watch && ./node_modules/.bin/nodemon ./dist/index.js",
    "build:watch": "./node_modules/.bin/babel -w ./src --out-dir ./dist &",
    "provision-server": "DEBUG=http babel-node ./bin/provision_server.js"
  },
  "dependencies": {
    "keystone": "^4.0.0-beta.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "doapi": "^0.1.4",
    "nodemon": "^1.12.0",
    "node-ssh": "^4.2.3"
  }
}
